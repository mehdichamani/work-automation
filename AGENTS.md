# 📚 سیستم مدیریت آموزشی - راهنمای سریع

## 🎯 بررسی کلی

سامانه اتوماسیون اداری اروم شیشه ساچی دارای یک سیستم کامل مدیریت محتوای آموزشی است که به شما امکان می‌دهد فایل‌های آموزشی (PDF و فیلم) را به اشتراک بگذارید. این سیستم از RBAC کامل بهره می‌برد که تنها به مدیران سیستم اجازه بارگذاری، ویرایش و حذف محتوا را می‌دهد، در حالی که همه کاربران می‌توانند به این محتوا دسترسی داشته باشند.

---

## 📁 ساختار سیستم

```
backend/
├── routes/
│   └── educational.js        # API های مدیریت آموزشی (180 خط)
├── uploads/
│   └── educational/          # پوشه ذخیره‌سازی فایل‌های آپلود شده
└── database/
    └── init.js              # تعریف جدول آموزشی و 6 ایندکس

frontend/
└── src/
    └── pages/
        ├── LearningViewer.jsx      # مشاهده محتوا برای همه کاربران
        └── EducationalManager.jsx  # مدیریت آموزشی برای ادمین‌ها
```

---

## 🚀 ویژگی‌های کلیدی

### 📄 قالب‌های پشتیبانی‌شده

| فرمت | توضیحات | حداکثر حجم | محدودیت |
|-------|-------------|-------------|----------|
| PDF   | اسناد آموزشی، راهنما، کتاب‌ها | 50 مگابایت | تمام فرمت‌های PDF |
| فیلم  | فیلم‌های آموزشی، سمینارها، کارگاه‌ها | 100 مگابایت | MP4، AVI، MOV، MKV، WebM |
| صوتی  | فایل‌های صوتی آموزشی، پادکست‌ها | 50 مگابایت | MP3، WAV، OGG، AAC، M4A |

### 🔐 کنترل دسترسی

**⚠️ نکته مهم:** تنها نقش‌های `ادمین` می‌توانند محتوا بارگذاری، ویرایش یا حذف کنند.

**مجوزهای پویا:** از سیستم حقوق و دسترسی‌های پویا (RBAC) موجود در سامانه بهره می‌برد که شامل:
- دسترسی برای تمام کاربران به محتوای فعال (`target_audience = 'all'`)
- دسترسی محدود به مدیران (`target_audience = 'manager'`)
- دسترسی محدود به سرپرست (`target_audience = 'supervisor'`)
- مجوزهای جداگانه برای ادمین‌ها

### 📊 قابلیت‌های گزارش‌گیری

* **پنل گزارش‌گیری ادمین** (`/admin/learning-stats`) شامل:
  - تعداد کل فایل‌های بارگذاری‌شده
  - توزیع بر اساس دسته‌بندی
  - روند بارگذاری (هفتگی/ماهانه)
  - حجم کل فایل‌های ذخیره‌شده
  - آمار بازدید (در صورت فعال بودن)

* **عملیات‌های گزارش‌گیری**:
  - فایل‌های پرطرفدار
  - معامله‌های اخیر (۲۴ ساعت گذشته)
  - فایل‌های در انتظار تأیید

---

## 🛠️ نحوه استفاده

### 🔍 برای کاربران عادی (مشاهده)

1. **دسترسی:** هر کاربر واردشده می‌تواند به `/learning` دسترسی داشته باشد
2. **مشاهده:** از پنل کناری **آموزش** برای مرور فایل‌های موجود استفاده کنید
3. **فیلتر کردن:** از نوار جستجو برای یافتن سریع فایل‌ها بر اساس عنوان، توضیحات یا برچسب‌ها استفاده کنید
4. **مشاهده:** روی هر فایل برای مشاهده جزئیات کلیک کنید

### ➕ برای مدیران سیستم (مدیریت)

1. **دسترسی:** فقط کاربران با نقش `ادمین` می‌توانند مدیریت آموزشی انجام دهند
2. **مدیریت:** از پنل کناری **مدیریت آموزشی** (فقط برای ادمین‌ها نمایش داده می‌شود) استفاده کنید
3. **بارگذاری:** از دکمه **بارگذاری فایل جدید** برای آپلود فایل‌های PDF یا فیلم استفاده کنید
4. **ویرایش:** از گزینه‌های سه نقطه برای هر فایل استفاده کنید و **ویرایش** یا **حذف** را انتخاب کنید

### 📤 فرآیند بارگذاری فایل

1. **انتخاب فایل:** PDF یا فیلم خود را انتخاب کنید (حداکثر حجم: 50 مگابایت برای PDF، 100 مگابایت برای فیلم)
2. **پر کردن فرم:** عنوان (الزامی)، توضیحات (اختیاری)، دسته‌بندی (الزامی)، مخاطب هدف، برچسب‌ها (اختیاری)
3. **ارسال:** فرم را ارسال کنید، سیستم به صورت خودکار فایل را اعتبارسنجی و پردازش می‌کند
4. **پیام موفقیت:** یک پیام تأیید موفقیت نمایش داده می‌شود همراه با اطلاعات دانلود

### 🔍 جستجو و فیلتر کردن

**جستجوی سریع:** از نوار جستجوی بالا استفاده کنید تا به سرعت فایل‌ها را بر اساس عنوان، توضیحات یا برچسب‌ها بیابید

**فیلترهای پیشرفته:**
- **دسته‌بندی:** برای مشاهده PDFها، فیلم‌ها یا هر گروه دیگری فیلتر کنید
- **برچسب‌ها:** از برچسب‌هایی مانند "تازه"، "مهم"، "پربازدید" برای دسته‌بندی سریع استفاده کنید
- **تاریخ:** فایل‌های جدیدترین، قدیمی‌ترین یا افزوده‌شده در هفته گذشته را مشاهده کنید

---

## 🎥 مشاهده محتوا

### مشاهده PDF

* مرورگر PDF داخلی با قابلیت‌های:
  - جستجو در متن
  - نشان‌گذاری و یادداشت‌برداری
  - تنظیم زوم
  - چاپ و دانلود
  - اشتراک‌گذاری

### تماشای فیلم

* پخش کننده ویدیویی کامل با قابلیت‌های:
  - پخش و توقف
  - پخش کامل
  - سرعت قابل تنظیم
  - زیرنویس (در صورت وجود)
  - صفحه‌گردان (در صورت وجود)
  - پخش خودکار اطلاعات در سمت راست (توضیحات) و پایین صفحه (اطلاعات فنی)

---

## 🔐 امنیت و ایمنی

### 🔒 ایمنی بازدید

* **HTTPS:** تمامی ترافیک بازدید از محتوا از طریق HTTPS رمزگذاری می‌شود
* **پوشه‌های امن:** فایل‌های بارگذاری‌شده در پوشه‌های امن سرور ذخیره می‌شوند، جدا از فایل‌های اصلی سامانه
* **لاگ فعالیت‌ها:** آمار بازدید ثبت می‌شود تا تخلفات احتمالی شناسایی شوند

### 🛡️ کنترل دسترسی

* **ثبت تمامی عملیات:** تمامی عملیات بارگذاری، ویرایش و حذف توسط ادمین‌ها ثبت می‌شوند
* **RBAC:** سیستم دارای کنترل دسترسی مبتنی بر نقش (RBAC) است
* **مخاطب‌های مخصوص:** هر فایلی می‌تواند به یک یا چند کاربر اختصاص داده شود که تنها آن کاربران مجاز به بازدید آن هستند

### 🧹 عملیات‌های نگهداری

* **پاکسازی خودکار:** سیستم به صورت خودکار فایل‌های بارگذاری‌شده با حجم زیاد یا فرمت‌های نامعتبر را شناسایی و حذف می‌کند
* **پشتیبان‌گیری:** فایل‌های قدیمی که بیش از ۱۰ بار بازدید شده‌اند، به صورت خودکار در فایل‌های بک‌آپ کپی می‌شوند

---

## 🆘 راهنمای رفع مشکلات

| مشکل | راه‌حل |
|---------|---------|
| **فرمت فایل پشتیبانی نمی‌شود** | فرمت فایل را بررسی کنید. فقط PDF و فیلم‌های رایج پشتیبانی می‌شوند |
| **حجم فایل بیشتر از حد مجاز است** | فایل را فشرده‌تر کنید یا آن را به چند بخش کوچک‌تر تقسیم کنید |
| **عدم دسترسی برای انجام این عمل** | درخواست کنید که یک ادمین به شما دسترسی بازدید بدهد |
| **فایل به درستی بارگذاری نشد** | اتصال اینترنت خود را بررسی کنید و تلاش کنید تا فایل دوباره بارگذاری شود |

---

## 📁 محدودیت‌های فنی

### محدودیت‌های سرور

* **فضای ذخیره‌سازی:** حداکثر ۵۰۰ مگابایت برای تمامی فایل‌های آموزشی
* **محدودیت درخواست‌ها:** ۱۰۰ درخواست در دقیقه برای بارگذاری
* **محدودیت حجم فایل:** 50 مگابایت برای PDF، 100 مگابایت برای فیلم، 50 مگابایت برای فایل‌های صوتی

### محدودیت‌های کلاینت

* **فرمت‌های پشتیبانی‌شده:** PDF (`.pdf`)، فیلم (`.mp4`, `.avi`, `.mov`, `.mkv`, `.webm`)، صوتی (`.mp3`, `.wav`, `.ogg`, `.aac`, `.m4a`)
* **حجم فایل کلاینت:** حداکثر ۱۵۰ مگابایت (برای جلوگیری از قطع شدن درخواست‌های بزرگ)
* **پشتیبانی ازبارگذاری چندفایلی:** حداکثر ۵ فایل در هر درخواست بارگذاری

---

## 🎯 بهترین روش‌ها

* **به‌روزرسانی:** فایل‌های قدیمی را مرتباً بررسی کنید و اگر منسوخ شده‌اند، به‌روزرسانی نمایید
* **برچسب‌گذاری:** فایل‌ها را با برچسب‌های مرتبط به‌صورت سیستماتیک برچسب‌گذاری کنید تا جستجو آسان‌تر شود
* **پشتیبان‌گیری:** فایل‌های مهم را در خارج از سرور پشتیبان‌گیری کنید
* **حفظ حریم خصوصی:** از بارگذاری اطلاعات شخصی حساس در فایل‌های آموزشی اجتناب کنید

---

## 📞 پشتیبانی

در صورت بروز هرگونه مشکلی در استفاده از بخش آموزشی، لطفاً به:

* **مسئول IT:** extension@yourcompany.com
* **پشتیبانی سامانه:** +98 21 12345678
* **تیکت:** از طریق پنل کاربری سامانه ثبت کنید

---

## 🔗 لینک‌های مرتبط

* **داشبورد اصلی:** `/dashboard`
* **مدیریت کاربران:** `/admin/users`
* **مدیریت مجوزها:** `/admin/permissions`
* **لاگ فعالیت‌ها:** `/admin/audit`

---

> **توجه:** این راهنما به صورت پویا به‌روزرسانی می‌شود. برای دسترسی به آخرین به‌روزرسانی‌ها، دکمه **تازه‌سازی** را کلیک کنید.

---

*آخرین به‌روزرسانی: ۲۰۲۶-۰۸-۰۵*
*محتوا: نسخه ۱.۰*
*نویسنده: تیم توسعه سامانه اتوماسیون اداری*

---

*برای بازگشت به داشبورد اصلی، لطفاً روی دکمه **بازگشت** در گوشه بالا-چپ کلیک کنید.*
